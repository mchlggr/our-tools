import React, {memo} from 'react';
import PropTypes from 'prop-types';


const LibraryModelDisplay = (props) => {
    return (
        <div></div>
    );
}

LibraryModelDisplay.propTypes = {
    
};

const LibraryModelDisplayMemo = memo(LibraryModelDisplay)
LibraryModelDisplayMemo.displayName = "LibraryModelDisplay"

export default LibraryModelDisplayMemo;